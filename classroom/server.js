const express = require("express");
const app = express();
// const user = require("./");
// const postes = require("./");
const cookieParser = require("cookie-parser");

app.listen(3000, () => {
    console.log("Server is listening to 3000");
});